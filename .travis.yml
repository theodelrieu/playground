language: cpp

dist: xenial

matrix:
  include:
    - os: linux
      compiler: gcc
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - ninja-build
            - python3
            - python3-venv
      env:
        - CONAN_PROFILE=gcc5
        - COMPILER=g++-5

    - os: linux
      compiler: gcc
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - ninja-build
            - python3
            - python3-venv
            - g++-6
      env:
        - CONAN_PROFILE=gcc6
        - COMPILER=g++-6

    - os: linux
      compiler: gcc
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - ninja-build
            - python3
            - python3-venv
            - g++-7
      env:
        - CONAN_PROFILE=gcc7
        - COMPILER=g++-7

    - os: linux
      compiler: gcc
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-8
            - ninja-build
            - python3
            - python3-venv
      env:
        - CONAN_PROFILE=gcc8
        - COMPILER=g++-8

    - os: linux
      compiler: clang
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
            - llvm-toolchain-precise-3.5
          packages:
            - clang-3.5
            - python3
            - python3-venv
            - ninja-build
      env:
        - CONAN_PROFILE=clang3.5
        - COMPILER=clang++-3.5

    - os: linux
      compiler: clang
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
            - llvm-toolchain-precise-3.6
          packages:
            - clang-3.6
            - python3
            - python3-venv
            - ninja-build
      env:
        - CONAN_PROFILE=clang3.6
        - COMPILER=clang++-3.6

    - os: linux
      compiler: clang
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
            - llvm-toolchain-precise-3.7
          packages:
            - clang-3.7
            - python3
            - python3-venv
            - ninja-build
      env:
        - CONAN_PROFILE=clang3.7
        - COMPILER=clang++-3.7

    - os: linux
      compiler: clang
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
            - llvm-toolchain-precise-3.8
          packages:
            - clang-3.8
            - python3
            - python3-venv
            - ninja-build
      env:
        - CONAN_PROFILE=clang3.8
        - COMPILER=clang++-3.8

    - os: linux
      compiler: clang
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
            - llvm-toolchain-precise-3.9
          packages:
            - clang-3.9
            - python3
            - python3-venv
            - ninja-build
      env:
        - CONAN_PROFILE=clang3.9
        - COMPILER=clang++-3.9

    - os: linux
      compiler: clang
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
            - llvm-toolchain-trusty-4.0
          packages:
            - clang-4.0
            - python3
            - python3-venv
            - ninja-build
      env:
        - CONAN_PROFILE=clang4
        - COMPILER=clang++-4.0

    - os: linux
      compiler: clang
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
            - llvm-toolchain-trusty-5.0
          packages:
            - clang-5.0
            - python3
            - python3-venv
            - ninja-build
      env:
        - CONAN_PROFILE=clang5
        - COMPILER=clang++-5.0

    - os: linux
      compiler: clang
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
            - llvm-toolchain-trusty-6.0
          packages:
            - clang-6.0
            - python3
            - python3-venv
            - ninja-build
      env:
        - CONAN_PROFILE=clang6
        - COMPILER=clang++-6.0

    - os: linux
      compiler: clang
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
            - llvm-toolchain-trusty-7
          packages:
            - clang-7.0
            - ninja-build
            - python3
            - python3-venv
      env:
        - CONAN_PROFILE=clang7
        - COMPILER=clang++

    - os: linux
      compiler: clang
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
            - llvm-toolchain-trusty-8.0
          packages:
            - clang-8.0
            - python3
            - python3-venv
            - ninja-build
      env:
        - CONAN_PROFILE=clang8
        - COMPILER=clang++-8.0

before_install:
  - export CXX=${COMPILER}
  - python3 ci/install-dmenv.py

install:
  - ./dmenv install

script:
  - ./dmenv run -- python ci/run-ci.py build-and-test --profile ${CONAN_PROFILE}
