code

* remove conan workspaces, create one repo per recipe
* mgs_detail regrouping everything that can be used by other packages + test_helpers

doc

* use 0.1.0
* migrate to readthedocs
* fix documentation about '\0'
* fix codec traits template args order

tests
* use a mpl library to match static asserts
