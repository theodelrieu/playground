mgs_meta:
    folder: meta
    includedirs: include

mgs_exceptions:
    folder: exceptions
    includedirs: include

mgs_cmake:
    folder: cmake

mgs_adapters_base:
    folder: adapters_base
    includedirs: include

mgs_adapters:
    folder: adapters
    includedirs: include

mgs_test_helpers:
    folder: test_helpers
    includedirs: include

mgs_binary_to_text:
    folder: binary_to_text
    includedirs: include

mgs_base64:
    folder: base64
    includedirs: include

mgs_base64url:
    folder: base64url
    includedirs: include

mgs_base32:
    folder: base32
    includedirs: include

mgs_base32hex:
    folder: base32hex
    includedirs: include

mgs_base16:
    folder: base16
    includedirs: include

mgs_codecs_base:
    folder: codecs_base
    includedirs: include

mgs_all:
    folder: all

root: mgs_all
generator: cmake
name: mgs
